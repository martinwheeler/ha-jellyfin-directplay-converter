# Release versioning

- Whenever this project's version is bumped for a release, commit the complete release changes and create the matching Git tag. For example, version `0.1.7` requires tag `v0.1.7` on that release commit.
- Verify that the tag points to the release commit before considering the version bump complete.
- Do not push commits or tags to a remote unless the user asks.
